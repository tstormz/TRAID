<%= form_for(:customer, :url => @tid_hash, :html=>{:class=>"form-horizontal"}) do |f| %>

  <%= render(:partial => "fields", :locals => {:f => f}) %>

  <%= submit_tag('Search', :class => "btn btn-primary") %>

<% end %>